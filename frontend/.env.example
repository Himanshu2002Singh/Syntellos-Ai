# Optional for a separately hosted backend. Local development proxies /api to port 5000.
# VITE_API_BASE_URL=https://your-backend.example.com/api
# If your backend runs on another local port, set its proxy target here.
# VITE_API_PROXY_TARGET=http://localhost:5001

# Optional WhatsApp destination number, country code and digits only.
# VITE_WHATSAPP_PHONE=919889505166
